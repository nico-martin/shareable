# Server Configuration
PORT=7777

# Node Environment
NODE_ENV=development

# Allowed Hosts (comma-separated list of allowed URLs/domains to render)
# Leave empty or set to * to allow all hosts (not recommended for production)
ALLOWED_HOSTS=http://localhost:3000,https://v26.uphillconf.com
